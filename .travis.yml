language: minimal

services: docker

env:
  - DOCKER_COMPOSE_VERSION=1.23.2

before_install:
  - docker -v && docker-compose -v
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin
  - curl -LO https://storage.googleapis.com/kubernetes-release/release/$(curl -s https://storage.googleapis.com/kubernetes-release/release/stable.txt)/bin/linux/amd64/kubectl
  - chmod +x ./kubectl
  - sudo mv ./kubectl /usr/local/bin/kubectl



install:
    - export AWS_ACCESS_KEY_ID='AKIA3EMTN7DW6DELG6EG'
    - export AWS_SECRET_ACCESS_KEY='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'
    - export KUBECONFIG='k8s-restapi-todo/todo-deployment/todo-app-kubeconfig'
    - docker-compose -f k8s-restapi-todo/todo-deployment/docker/docker-compose-build.yaml build --parallel 
    - docker login -u "programmingwebsite" -p "c9f12b41-e014-42cb-84b2-f71b41353962" docker.io
    - docker-compose -f docker-compose-build.yaml push


